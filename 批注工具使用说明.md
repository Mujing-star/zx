# 页面批注工具 · 使用说明

## 这是什么

一套直接叠加在原型页面上的批注浮层。**批注数据就保存在每个 HTML 文件自己内部**
（文件末尾的 `annoEmbeddedData` 数据块里），所以只要把文件提交到 Git 分支，
同事拉取分支、双击打开页面，就能看到你标的每一条批注——不需要注册任何账号，
不需要联网，不需要装插件。

## 文件组成

| 文件 | 作用 | 注意事项 |
|---|---|---|
| `annotation-tool.js` | 批注工具本体，所有页面共用 | 不要删，不要改名 |
| 各 HTML 末尾的 `annoEmbeddedData` 数据块 | 该页面的批注数据 | 随 Git 走，同事可见 |

页面本身的代码、样式、功能完全没有被改动，工具只是末尾多了两行引用。

## 日常使用（四步）

1. **写批注**：打开页面 → 右下角「✎ 添加批注」→ 点击要批注的元素 → 输入内容 → 保存。
   此时标记是**红色**，只存在你自己浏览器里（刷新不丢，但同事看不到）。
2. **保存到文件**：右下角「批注列表」→「保存到 HTML 文件」。
   标记变**蓝色**，表示已经写进 HTML 文件本身。
   - 用 Chrome / Edge：直接把批注写回项目里的文件。第一次会让你选择一次
     对应的 HTML 文件并授权，之后记住了不用再选。
   - 用其他浏览器（如 Safari）：会自动改为下载一个完整 HTML，
     用它替换项目里的同名文件即可。
3. **提交分支**：`git add -A && git commit && git push`，推到你自己的分支。
4. **同事查看**：同事切到你的分支，打开页面就能看到所有蓝色批注，
   点标记看内容，列表里可以「定位」到对应元素。

## 标记颜色

| 颜色 | 含义 |
|---|---|
| 红 | 未保存到文件（仅自己可见） |
| 蓝 | 已保存到文件（同事可见） |
| 橙 | 文件里已有、但你本地又改过还没保存 |

## 其他功能

- **编辑 / 删除**：批注列表里每条都有。删除已入文件的批注后，需要再点一次
  「保存到 HTML 文件」才会同步到文件。
- **导出 Markdown**：把本页批注导成 `.md` 文档，适合发给开发逐条处理。
- **清空**：清空本页所有批注（同样要保存到文件才对同事生效）。

## 主线更新后怎么办（重点）

主线（main）每次出新版本页面后，给新版本加批注的方法：

**推荐做法：把新文件交给 Codex。** 直接说一句「给新页面装上批注工具」即可，
会自动完成注入并检查旧批注是否还有效。

**手动做法**（自己也能做，30 秒）：

1. 确认 `annotation-tool.js` 和新 HTML 在同一目录；
2. 用编辑器打开新 HTML，在**最末尾的 `</body>` 之前**加上这两行：

```html
    <!-- ===== 页面批注工具（审阅用；批注保存在 annoEmbeddedData 数据块里，随文件提交 Git。删除下面两行即完全移除） ===== -->
    <script type="application/json" id="annoEmbeddedData">[]</script>
    <script src="annotation-tool.js"></script>
```

3. 保存，双击打开页面，右下角出现「✎ 添加批注」即成功。

### 旧批注的迁移

- 批注是**按页面元素位置**（CSS 选择器）记录的。如果主线只是小改，
  旧批注大多还能对上；如果页面结构大改，部分旧批注可能定位不到元素
  （批注内容还在列表里，只是标记不显示/定位失败）。
- 如果把旧文件里的批注带到新版本：找 Codex 说「把旧批注迁到新页面」，
  会逐条检查定位并迁移；手动方式是把旧文件 `annoEmbeddedData` 数据块里的
  JSON 复制到新文件的同位置数据块里。
- 用 Git 合并主线时，`annoEmbeddedData` 数据块可能出现冲突——这是正常的，
  把两边的批注条目合并保留即可（JSON 数组，注意逗号），
  或者直接交给 Codex 解决。

## 注意事项

- 批注按「页面文件名」区分，三个端互不干扰；**改文件名后旧批注会找不到页面**，
  改名前先导出 Markdown 备份。
- 红色的未保存批注存在浏览器 localStorage 里：同一台电脑同一个浏览器有效；
  换浏览器/换电脑前记得先「保存到 HTML 文件」。
- 同事如果也在同一页面加批注并保存，合并分支时数据块可能冲突，处理方式同上。
- 想彻底移除工具：删掉每个 HTML 末尾那两行，再删 `annotation-tool.js`，
  页面就和原来一模一样。

## 验证状态

工具经过自动化测试（真实 Chrome 环境）：添加/编辑/删除/刷新保留/页面功能不受影响/
保存到文件/同事打开可见/降级导出，全部通过。
